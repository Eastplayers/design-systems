export enum InputStyles {
  TOP_LABEL = "top label",
  CONTAINED_LABEL = "contained label",
  NO_LABEL = "no label"
}

export enum InputPosition {
  PREFIX = "prefix",
  SUFFIX = "suffix",
  NONE = "none"
}
